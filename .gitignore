/target
Cargo.lock
**/*.rs.bk
.DS_Store

# C reference oracle — dev-only, kept locally, not published to the repo.
# (Upstream pikchr.y is linked from the README.)
/reference/

# Internal spec — kept locally, not published to the repo
/TZ_pikchr_rust_port.md
